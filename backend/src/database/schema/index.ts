import { equipes, equipesUsuarios } from './equipes.ts'
import { torneios } from './torneios.ts'
import { usuarios } from './usuarios.ts'

export const schema = {
	usuarios,
	torneios,
	equipes,
	equipesUsuarios,
}
